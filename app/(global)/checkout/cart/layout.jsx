
export const metadata = {
  title: 'Grocery Shop | Shopping Cart',
  description: '...',
};
const layout = ({ children }) => {
  return (
    <div>
      {children}
    </div>
  );
};

export default layout;
